import 'package:flutter/material.dart';

/// Light Mode için renk paleti
class LightColors {
  // === PRIMARY COLORS ===
  static const primary = Color(0xFF1976D2);
  static const onPrimary = Color(0xFFFFFFFF);
  static const primaryContainer = Color(0xFFD3E4FD);
  static const onPrimaryContainer = Color(0xFF001C38);

  // === SECONDARY COLORS ===
  static const secondary = Color(0xFF535E71);
  static const onSecondary = Color(0xFFFFFFFF);
  static const secondaryContainer = Color(0xFFD7E3F7);
  static const onSecondaryContainer = Color(0xFF101C2B);

  // === TERTIARY COLORS ===
  static const tertiary = Color(0xFF6B5778);
  static const onTertiary = Color(0xFFFFFFFF);
  static const tertiaryContainer = Color(0xFFF2DAFF);
  static const onTertiaryContainer = Color(0xFF251431);

  // === BACKGROUND & SURFACE ===
  static const background = Color(0xFFFDFBFF);
  static const onBackground = Color(0xFF1A1C1E);
  static const surface = Color(0xFFFDFBFF);
  static const onSurface = Color(0xFF1A1C1E);
  static const surfaceVariant = Color(0xFFE0E2EC);
  static const onSurfaceVariant = Color(0xFF43474E);

  // === ERROR COLORS ===
  static const error = Color(0xFFBA1A1A);
  static const onError = Color(0xFFFFFFFF);
  static const errorContainer = Color(0xFFFFDAD6);
  static const onErrorContainer = Color(0xFF410002);

  // === SUCCESS COLORS ===
  static const success = Color(0xFF2E7D32);
  static const onSuccess = Color(0xFFFFFFFF);
  static const successContainer = Color(0xFFC8E6C9);
  static const onSuccessContainer = Color(0xFF1B5E20);

  // === WARNING COLORS ===
  static const warning = Color(0xFFF57C00);
  static const onWarning = Color(0xFFFFFFFF);
  static const warningContainer = Color(0xFFFFE0B2);
  static const onWarningContainer = Color(0xFFE65100);

  // === INFO COLORS ===
  static const info = Color(0xFF0288D1);
  static const onInfo = Color(0xFFFFFFFF);
  static const infoContainer = Color(0xFFB3E5FC);
  static const onInfoContainer = Color(0xFF01579B);

  // === OUTLINE & BORDERS ===
  static const outline = Color(0xFF74777F);
  static const outlineVariant = Color(0xFFC4C6D0);
  static const shadow = Color(0xFF000000);
  static const scrim = Color(0xFF000000);

  // === COMPONENT SPECIFIC COLORS ===
  // AppBar
  static const appBarBackground = Color(0xFFFDFBFF);
  static const appBarForeground = Color(0xFF1A1C1E);

  // Bottom Navigation Bar
  static const bottomNavBackground = Color(0xFFFFFFFF);
  static const bottomNavSelected = Color(0xFF1976D2);
  static const bottomNavUnselected = Color(0xFF74777F);

  // Cards
  static const cardBackground = Color(0xFFFFFFFF);
  static const cardShadow = Color(0x1A000000);
  static const cardBorder = Color(0xFFE0E2EC);

  // Buttons
  static const buttonPrimary = Color(0xFF1976D2);
  static const buttonSecondary = Color(0xFF535E71);
  static const buttonDisabled = Color(0xFFE0E2EC);
  static const onButtonDisabled = Color(0xFFA8A8A8);

  // Text Fields
  static const textFieldBackground = Color(0xFFF5F5F5);
  static const textFieldBorder = Color(0xFFE0E2EC);
  static const textFieldBorderFocused = Color(0xFF1976D2);
  static const textFieldBorderError = Color(0xFFBA1A1A);
  static const textFieldHint = Color(0xFF74777F);
  static const textFieldText = Color(0xFF1A1C1E);
  static const textFieldLabel = Color(0xFF535E71);

  // Dividers
  static const divider = Color(0xFFE0E2EC);
  static const dividerThick = Color(0xFFC4C6D0);

  // Chips & Tags
  static const chipBackground = Color(0xFFE8F0FE);
  static const chipForeground = Color(0xFF1976D2);
  static const chipBorder = Color(0xFFD3E4FD);

  // Icons
  static const iconPrimary = Color(0xFF1A1C1E);
  static const iconSecondary = Color(0xFF74777F);
  static const iconDisabled = Color(0xFFC4C6D0);

  // Overlay & Backdrop
  static const overlay = Color(0x52000000);
  static const backdrop = Color(0x99000000);

  // Status colors
  static const disabled = Color(0xFFE0E2EC);
  static const inactive = Color(0xFFC4C6D0);

  LightColors._(); // Private constructor to prevent instantiation
}
