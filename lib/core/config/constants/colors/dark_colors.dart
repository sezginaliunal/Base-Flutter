import 'package:flutter/material.dart';

/// Dark Mode için renk paleti
class DarkColors {
  // === PRIMARY COLORS ===
  static const primary = Color(0xFF9FCBFF);
  static const onPrimary = Color(0xFF003258);
  static const primaryContainer = Color(0xFF00497D);
  static const onPrimaryContainer = Color(0xFFD3E4FF);

  // === SECONDARY COLORS ===
  static const secondary = Color(0xFFBBC7DB);
  static const onSecondary = Color(0xFF253140);
  static const secondaryContainer = Color(0xFF3C4858);
  static const onSecondaryContainer = Color(0xFFD7E3F7);

  // === TERTIARY COLORS ===
  static const tertiary = Color(0xFFD6BEE4);
  static const onTertiary = Color(0xFF3B2948);
  static const tertiaryContainer = Color(0xFF52405F);
  static const onTertiaryContainer = Color(0xFFF2DAFF);

  // === BACKGROUND & SURFACE ===
  static const background = Color(0xFF1A1C1E);
  static const onBackground = Color(0xFFE2E2E5);
  static const surface = Color(0xFF1A1C1E);
  static const onSurface = Color(0xFFE2E2E5);
  static const surfaceVariant = Color(0xFF43474E);
  static const onSurfaceVariant = Color(0xFFC4C6D0);

  // === ERROR COLORS ===
  static const error = Color(0xFFFFB4AB);
  static const onError = Color(0xFF690005);
  static const errorContainer = Color(0xFF93000A);
  static const onErrorContainer = Color(0xFFFFDAD6);

  // === SUCCESS COLORS ===
  static const success = Color(0xFF81C784);
  static const onSuccess = Color(0xFF1B5E20);
  static const successContainer = Color(0xFF2E7D32);
  static const onSuccessContainer = Color(0xFFC8E6C9);

  // === WARNING COLORS ===
  static const warning = Color(0xFFFFB74D);
  static const onWarning = Color(0xFFE65100);
  static const warningContainer = Color(0xFFF57C00);
  static const onWarningContainer = Color(0xFFFFE0B2);

  // === INFO COLORS ===
  static const info = Color(0xFF4FC3F7);
  static const onInfo = Color(0xFF01579B);
  static const infoContainer = Color(0xFF0288D1);
  static const onInfoContainer = Color(0xFFB3E5FC);

  // === OUTLINE & BORDERS ===
  static const outline = Color(0xFF8E9099);
  static const outlineVariant = Color(0xFF43474E);
  static const shadow = Color(0xFF000000);
  static const scrim = Color(0xFF000000);

  // === COMPONENT SPECIFIC COLORS ===
  // AppBar
  static const appBarBackground = Color(0xFF2B2D30);
  static const appBarForeground = Color(0xFFE2E2E5);

  // Bottom Navigation Bar
  static const bottomNavBackground = Color(0xFF2B2D30);
  static const bottomNavSelected = Color(0xFF9FCBFF);
  static const bottomNavUnselected = Color(0xFF8E9099);

  // Cards
  static const cardBackground = Color(0xFF2B2D30);
  static const cardShadow = Color(0x33000000);
  static const cardBorder = Color(0xFF43474E);

  // Buttons
  static const buttonPrimary = Color(0xFF9FCBFF);
  static const buttonSecondary = Color(0xFFBBC7DB);
  static const buttonDisabled = Color(0xFF43474E);
  static const onButtonDisabled = Color(0xFF5F5F5F);

  // Text Fields
  static const textFieldBackground = Color(0xFF2B2D30);
  static const textFieldBorder = Color(0xFF43474E);
  static const textFieldBorderFocused = Color(0xFF9FCBFF);
  static const textFieldBorderError = Color(0xFFFFB4AB);
  static const textFieldHint = Color(0xFF8E9099);
  static const textFieldText = Color(0xFFE2E2E5);
  static const textFieldLabel = Color(0xFFBBC7DB);

  // Dividers
  static const divider = Color(0xFF43474E);
  static const dividerThick = Color(0xFF5F5F5F);

  // Chips & Tags
  static const chipBackground = Color(0xFF1F3A52);
  static const chipForeground = Color(0xFF9FCBFF);
  static const chipBorder = Color(0xFF00497D);

  // Icons
  static const iconPrimary = Color(0xFFE2E2E5);
  static const iconSecondary = Color(0xFF8E9099);
  static const iconDisabled = Color(0xFF5F5F5F);

  // Overlay & Backdrop
  static const overlay = Color(0x52000000);
  static const backdrop = Color(0xCC000000);

  // Status colors
  static const disabled = Color(0xFF43474E);
  static const inactive = Color(0xFF5F5F5F);

  DarkColors._(); // Private constructor to prevent instantiation
}
